coverage:
  paths:
    - coverage.out

diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
  acceptable: 60%

summary:
  if: true

report:
  if: is_pull_request
  datastores:
    - artifact://${GITHUB_REPOSITORY}
