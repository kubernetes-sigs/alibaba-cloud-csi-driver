name: coverage
on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  coverage:
    name: incremental coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          fetch-depth: 0
          filter: blob:none
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: 'go.mod'

      - name: Get changed Go packages
        if: github.event_name == 'pull_request'
        id: changed
        run: |
          FILES=$(git diff --name-only --diff-filter=ACMR ${{ github.event.pull_request.base.sha }}..HEAD -- '*.go' | grep -v vendor/ || true)
          if [ -z "$FILES" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            # Extract unique package paths from changed files (including test files)
            PKGS=$(echo "$FILES" | xargs -I{} dirname {} | sort -u | sed 's|^|./|' | paste -sd ' ' -)
            echo "packages=$PKGS" >> "$GITHUB_OUTPUT"
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Run tests with coverage (PR)
        if: github.event_name == 'pull_request' && steps.changed.outputs.skip != 'true'
        run: |
          sudo -E env "PATH=$PATH" go test -coverprofile=coverage.out -covermode=atomic ${{ steps.changed.outputs.packages }}

      - name: Run tests with coverage (push to master)
        if: github.event_name == 'push'
        run: |
          sudo -E env "PATH=$PATH" go test -coverprofile=coverage.out -covermode=atomic ./...

      - name: Report coverage
        if: steps.changed.outputs.skip != 'true' || github.event_name == 'push'
        uses: k1LoW/octocov-action@b3b6ee60482a667950f87553abf1df63217235d9 # v1.5.1
