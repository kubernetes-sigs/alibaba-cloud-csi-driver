apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: juicefs-configmap-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: juicefs-configmap-pv
  storageClassName: ""
