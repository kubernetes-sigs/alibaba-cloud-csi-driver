apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: juicefs-oss-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: juicefs-oss-pv
  storageClassName: ""
