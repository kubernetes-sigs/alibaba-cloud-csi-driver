apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: juicefs-standard-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: juicefs-standard-pv
  storageClassName: ""
