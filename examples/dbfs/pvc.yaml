apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-dbfs-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      alicloud-pvname: csi-dbfs-data